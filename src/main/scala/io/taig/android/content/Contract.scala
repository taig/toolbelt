package io.taig.android.content

/**
 * A contract may be concluded between an Activity (debtor) and a Fragment (creditor)
 * 
 * The Fragment defines the contract, the Activity has to implement it
 */
trait Contract